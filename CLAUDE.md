# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Visibility: public *(deliberately public from the start — owner policy 2026-07-09: science projects are public from birth, and the repo doubles as résumé/portfolio evidence. Consequence: public-grade discipline is MANDATORY — never commit personal data, ARR/reviewer text (`text_docs/reviews/` is gitignored), task files, secrets, or secret-manager references.)*

## Project

Research home for the **model-internals safety** line: safety research that operates INSIDE model parameters — training-time interventions (safety fine-tuning, unlearning) and parameter/activation-space work (activation steering, representation-level analysis and edits). **Model-size-agnostic by design:** the pipeline is the same for a 1B and a 70B; small open models are the default experimental substrate (cheaper runs, more seeds). Like its siblings, this is a **shared home for a line of work**, not a single paper.

**Founded 2026-08-02** (owner-ratified, from a science-organ session). The defining property vs the siblings: they are eval-only on frozen models (black-box inputs/outputs); THIS repo goes inside the model — a new cost shape (GPU training/intervention runs rather than API-call evals).

## Scope boundary (load-bearing — read before deciding where work goes)

- **THIS repo owns everything INSIDE the model:** training-time safety interventions, weight/activation-space methods, internals-level analysis of safety behavior, and their evaluation.
- **`llm_guardrail_security`** owns the black-box model-side line (encoding/imaging jailbreak attacks and content-guard defenses on frozen models; papers AS-1…AS-4, aliases A–D). **`llm_agent_security`** owns the agent line. The **science organ** (`personal/science`) owns cross-project research knowledge (venue records, the shared literature corpus).
- **One concrete example each side:** "LoRA-fine-tune a 7B on refusal data and measure generalization to unseen encodings" = **THIS repo**; "test whether a decoy image lowers a frozen VLM's ASR" = the guardrail sibling.
- If this line needs the family's encoders as eval payloads, they get **COPIED**, never imported (oikos charter bars research-bet→research-bet dependencies; rule of two → extract a standalone package).

## Status

The first paper (Paper AS-5, alias E, "Can't, didn't, or wouldn't?") is at **S1 complete — direction committed, S2 feasibility open** — see `text_docs/as5/proposal.md` (its `Workflow stage:` line is the state of record) and `text_docs/as5/s1_idea_check.md`.

**This repo went TWO-PAPER on 2026-08-05 (owner order).** The two are cut by **object**, not by substrate or by which sibling the work came from: **AS-5 measures the chain inside the *target*** (can't / didn't / wouldn't), and **AS-6 measures the same chain inside the *defense*** — separating *never decoded* from *decoded but never blocked* in a guard's own activations, which end-to-end ASR structurally confounds. AS-6 has been **scoop-checked (Level 3, open)**, passed its S1 external idea check, and — after a hostile review rejected its first scoping — **routed to a reshaped phase 1: forward passes only, no training, $0** (`text_docs/as6/s0_reshape.md` §6). Its scope of record is `text_docs/as5/proposal.md` §"AS-6 — the second paper" (misplaced in the as5 namespace; move filed), its card is science `portfolio.md`, and its task line with a cold-resume briefing is TODO item 9. **Owner ruled 2026-08-05 that AS-6 continues while AS-5 is mid-flight** — paper count on this line is settled at two.

**AS-6 landed its first code 2026-08-05** (`73c1f55`): `src/internals_safety/guards/` + `conf/guards/`, the guard prompt layer, which existed because `render_chat` fails closed on WildGuard (it ships no chat template — now verified against the artifact, not inferred). Three facts any session touching guards must not re-derive: WildGuard's format is upstream's `WILDGUARD_INPUT_FORMAT` **verbatim from the library constant** (the model card differs by `<s>`, since upstream lets vLLM add BOS and we tokenise `add_special_tokens=False`); its verdict is **not token 0** — it answers `Harmful request: <verdict>`, so a first-token readout scores P("Harmful") ≈ 1 under either verdict; and **Llama Guard 3's shipped template emits a stray leading SPACE before `<|begin_of_text|>`**, so token 0 is whitespace and BOS is token 1. All pinned in `tests/test_real_guard_tokenizers.py` (tokenizers only — no weights, seconds). Both gated repos are **already licensed** on this account; the feasibility spike's "owed auth wall" was not real.

Three consequences for any session in this repo: (a) the two papers **share the instrument layer**, so a probe/regime/coherence change is never AS-5-only — the coherence check's `recognition_without_deployment` rule is AS-6's core instrument too; (b) the **length null (TODO 17) binds AS-6 from its first line of code**, not as a retrofit — the AS-5 probe was reading character length at AUROC 0.654 on 12 of 15 rungs, and Broken-Token's characters-per-token null is a *different* control that does not cover it; (c) the namespace-subdir trigger has now **fired** — settle it in one deliberate pass (TODO item 20), reading the sibling convention first, and noting this repo's cut is by OBJECT: `probes/`, `measurements/`, `models/` are shared instrument and only object-of-study layers like `guards/` are paper-specific.

Code has started, ahead of S2, because the phase-0 pilot is itself the S2 gate. **Build steps 1–4 of `text_docs/project_structure.md` §6 are done**: the forward-pass spine, the 15-rung encoding ladder, the probe layer, the judge layer — all four measurements of `s1_idea_check.md` §7 — and `scripts/phase0_regime_map.py`, which combines them into a per-(model, family) regime map.

**The pilot RAN 2026-08-03 (Qwen2.5-7B + Llama-3.1-8B, 15 rungs × 100 prompts, 4 SLURM jobs on H200) and its gating question is ANSWERED: the (B) decode-and-comply cell IS populated — but on one rung only, and only after an instrument fix.** What it establishes:

- **MOST of the cipher band is inert — but NOT all of it, and the earlier blanket claim here was FALSE (corrected 2026-08-05).** This bullet previously read "across the twelve deterministic cipher rungs … mean decode-and-restate similarity 0.03–0.30 … uniformly (R) surface refusal." That range silently excluded the two rungs that break it. Recomputed directly from `outputs/runs/phase0/*/cells.jsonl`:
  - **Ten of the twelve cipher rungs are genuinely inert** (base64, base32, rot13, caesar3, caesar7, atbash, vigenere, morse, binary, ascii_decimal): mean similarity 0.03–0.30, **zero** cells above threshold on either model. For these, *can't* is the uniform and correct answer.
  - **`hex` and `unicode_escape` are readable.** Llama-3.1-8B: hex **mean 0.790, 72/100** cells ≥ 0.75; unicode_escape **mean 0.699, 53/100**. Qwen2.5-7B: hex 0.484 (25/100 ≥ 0.60), unicode_escape 0.465 (23/100 ≥ 0.60). Spot-checked restatements are genuine near-verbatim decodes (e.g. sim 0.981 recovering the exact plaintext) that the **old `exact or contains` ability binary scored `ability=False`, `regime=R`** on a one-word slip.
  - **Consequence — a natural decode gradient already exists on disk, paid for, at n=100 per point:** on Llama, 0 → 53 → 72 → 78 → 99 (per 100) across unicode_escape → hex → reverse_words → zero_width. `unicode_escape` on Llama and `hex` on Qwen sit *at the edge of a specific model's ability*, which is the condition an expensive intervention would otherwise have to manufacture.
  - **This revises the AS-5 regime map — but NOT in the direction first stated here.** An earlier version of this bullet said hex cells that decoded and refused become **(S)**. **That was wrong** (corrected after the re-baseline actually ran; record: `text_docs/as5/pilot_rebaseline.md` §3). Ability on Llama `hex` does go 21→84, but `hex`'s **deployment probe is unlicensed** (transfer AUROC 0.691), so pre-fix every cell read `deployment=false` and the regime stayed `(R)`. The honest label is **neither (R) nor (S) — the rung is unmeasurable on the deployment axis**, and since the tri-state fix landed the same day it now correctly reads **(U)**. The cipher band's uniform (R) was *unsupported*, not *wrong in a known direction*.
- **Where comprehension happens, the four-regime frame works.** `zero_width` (mean similarity 0.987) reads **B=7/S=93 on Llama and B=13/S=86 on Qwen** with hard incoherence at 0–1% (re-baselined figures — the pre-fix range recorded here was B=5–13, S=86–95, which the re-derivation lands within a couple of cells of). Models that genuinely decode overwhelmingly decode *and refuse*, with a small real binding-failure tail. The (B) count is **threshold-invariant across τ=0.75–0.90**, so it does not hinge on the knob.
- **RE-BASELINED 2026-08-05 — `text_docs/as5/pilot_rebaseline.md` is now the regime map of record.** Two offline scripts, both keyless and GPU-free, answering different questions: `scripts/rescore_ability.py` asks "what would thresholding similarity do?" and self-checks against the shipped labels — that self-check is now **RED by design** (74/1500 cells, `reverse_characters recorded=X recomputed=R`), because `cells.jsonl` predates instrument fixes #1/#2; the guard is working. `scripts/rebaseline_pilot.py` asks "what does the CURRENT instrument say about the cached data?" — it recomputes ability from cached text under the settled rule, restores recognition's tri-state from per-family licensing in `results.json`, and re-labels with the current `assign_regime`. **Result: the gating headline survives** (zero_width B=7/S=93 Llama, B=13/S=86 Qwen, hard incoherence 0–1%), ability moved **+225/−0** on Llama and **+103/−0** on Qwen, and `reverse_characters` went 71% hard incoherence → 0.
- **⚠️ INSTRUMENT DEFECT FOUND AND FIXED 2026-08-05 — `deployment` was not tri-state, and it was the most consequential defect in the pilot.** `recognition` is; `deployment` is not — yet `results.json` records `deployment.licensed = false` on **13 of 15 Llama rungs and every rung Qwen completed**, while those cells all carry a plain `deployment=false`. That is exactly the failure tri-state fixed for recognition, one measurement over: an unlicensed probe asserting *"the model did not decode during the attack"* when the truth is *"this instrument could not read this rung"*. Since `deployment` is the term separating (S)/(B) from (R)/(D), **the cipher band's uniform (R) is an artefact of a silent `False`, not a measurement.** AS-6 inherits this at its core — deployment *is* AS-6's central quantity. **FIXED end to end the same day:** `DeploymentReading` returns `None` per prompt on an unlicensed rung, the rate/gap properties return `None` rather than `0.0`, `assign_regime` skips rules reading an unmeasured axis and returns the new regime **(U)**, `RegimeMap` gains `deployment_unmeasured`, and `binding_failure_rate` is computed over MEASURED cells returning `None` when there are none (dividing by `n` gave `0.0` = "no binding failures here" — the same silent zero one level up). 226 tests green.
- **The honest map is TWO RUNGS, not fifteen.** With the fix applied, `zero_width` and `reverse_words` are the only rungs on either model whose deployment probe licensed — **13 of 15 Llama rungs and every Qwen cipher rung are (U)**. The (B) cell survives on both measured rungs (zero_width B=7/S=93 Llama, B=13/S=86 Qwen). Map of record: `text_docs/as5/pilot_rebaseline.md`.
- **⚠️ THE LENGTH CONFOUND (found 2026-08-05, re-licensing run — read before trusting ANY probe number).** Re-licensing deployment under the permutation test moved it from 2/15 to 14/15 rungs on Llama. That is NOT the map growing: **raw character length alone separates the harmful from the benign corpus at AUROC 0.6544** (JBB harmful 86.0 chars vs benign 73.8), every encoder is monotone in length so the separation survives into every rung, and the newly-licensed rungs sit at **mean deployment AUROC 0.659 against mean length AUROC 0.654** — indistinguishable. Confirmed three ways: the newly-licensed rungs include ones with **ability 0/100** (nothing was decoded, so nothing could be deployed); `deployment ✓ + ability ✗` is a HARD incoherence by the module's own rule; and the two genuine rungs beat the length null by 0.19–0.29 (`reverse_words` 0.844, `zero_width` 0.945) while nothing else beats it at all. **§8's format-decorrelation control cannot catch this** — it defeats "looks encoded" by sending benign content through the same pipeline, but length is a *content-correlated* property the encoder preserves. **A length null model is now mandatory for both papers** (TODO 17); it is the same control AS-6's reshape already names for the tokenizer, now shown necessary on the AS-5 side empirically. Permutation licensing controls the multiple comparison; it says nothing about WHAT separates. Record: `text_docs/as5/pilot_rebaseline.md` §5.
- **RESOLVED 2026-08-07 — re-licensing ran (`relicense_all`, job `8995184`, 29/30 tasks, CPU, $0) and the map stays at TWO rungs.** The hope was that `hex` at 0.691 "a hair under the old 0.70 cut" would re-license and hand phase 1 a natural decode gradient. It does re-license — **and so does nearly everything: 14/15 rungs on Llama and 14/14 on Qwen pass the permutation test.** Significance is not sufficiency, measured a second time. Screened against the run's own can't-decode rungs (11 on Llama, 10 on Qwen), only `zero_width` and `reverse_words` clear, **on both models**: 0.945/0.844 (Llama), 0.989/0.870 (Qwen), against control ceilings of 0.674/0.683. `hex` clears on Llama by +0.017 and fails on Qwen by −0.021; every floor ever applied to it lands within ±0.02. **Phase 1 must build its intervention substrate deliberately — there is no natural gradient to harvest.** Record: `instrument_layer.md` §2.4; reproduce with `scripts/control_floor.py`; artifacts `outputs/analysis/relicense_20260807/` + `control_floor_20260807.json`.
- **⚠️ Two things that run found, which matter more than the gate answer.** (a) **The control floor's MAX statistic is n-dependent** — the same instrument on the same models moved 0.656→0.674 (Llama) and 0.671→0.683 (Qwen) purely by going from a 2-rung control set to an 11/10-rung one, so floors from different runs are **not comparable** and §2.2's table must never be imported. **ADOPTED the same day (owner go): the rule is `mean + sigma*SD`, sigma = 2.0** (`measurements/control_floor.py`, knobs `controls.control_floor_sigma` / `control_floor_min_controls`). Sigma is a DERIVED WINDOW, not a chosen number: *no control rung may clear its own floor* forces sigma >= 1.531, *both genuine rungs still pass* caps it at 6.17, and nothing in that range changes a conclusion. `hex` sits at k = 1.383 — below the bar the no-control-may-pass requirement imposes before 2.0 is chosen at all, which is what makes the adoption non-circular. Below 5 controls the floor falls back to max and is LABELLED a `bound`. (b) **The strongest statement of the deployment confound this repo has:** Llama decodes **84 of 100** `hex` prompts and the deployment probe reads 0.691 — the value it reads on rungs the model cannot decode at all; on `unicode_escape` it decodes 54% and the probe reads *below* that ceiling. Established within one run against its own control, no external length null needed.
- **⚠️ LICENSING counts from every AS-5 run before 2026-08-07 are under the retired null and may not be reported — FIXED the same day.** The length-matched null settled 2026-08-06 reached `measure_deployment` and AS-6's sweep, and **neither `relicense_probes.py` nor `phase0_regime_map.py`** — the main AS-5 entrypoint — so the gap was wider than first filed. Third instance of *a settled rule that did not reach every caller*. **The fix is structural: `strata` is keyword-only with NO default**, so omitting it is a `TypeError`, and all three callers build it (`tests/test_control_floor.py::TestTheSupersededNullCannotBeReachedByOmission` pins signature and callers both). The lesson: threading a rule into its callers is not the fix — making the omission inexpressible is. The two-rung conclusion is unaffected (the control-floor screen never consults a p-value); AS-5 re-licensing under the matched null is now a re-run, not a code change.
- **⚠️ Ability figures quoted above use a retired cut.** Recomputed under the settled cuts (similarity 0.75): Llama `hex` **0.84** (this file says 72/100) and Qwen `hex` **0.01** (this file says 25/100 — that figure used a 0.60 cut). Under the settled rule `hex` and `unicode_escape` are Llama-readable and Qwen-inert.
- **Consequence for the ladder (acted on 2026-08-04; the comprehension-preserving claim PARTLY REFUTED 2026-08-05 — see below):** the ordering variable is **surface conspicuousness**, not decoding difficulty. Four EXACT rungs were added — `tag_block`, `homoglyph`, `fullwidth`, `combining_marks` (`encodings/deterministic/surface.py`) — making 19. The next sweep should run the **7-rung comprehension band**, not all 19: 2.2–3.8 GPU-h and $0.77–2.31 per model, inside the 8 h wall, versus 5.8–10.3 GPU-h for the full ladder, which exceeds it.
- **Two instrument defects — BOTH LANDED 2026-08-05** (`0483fe8`, "all three blocked rungs now read coherently"; 439 cells newly count as ability). Fix #1 replaced the `exact or contains` ability binary with three routes to recovery (exact/contains short-circuit, similarity ≥ 0.75 with a content-overlap veto, and an order-blind overlap branch for `reverse_words`), cuts in `conf/measurements.yaml`. Fix #2 addressed recognition/deployment disagreement, which was confirmed independent. **What remains is calibration and re-baselining, not landing** — and the correction above shows fix #1 was more consequential than recorded: it is what was hiding `hex` and `unicode_escape`.
- **Reproducibility signal:** re-running the same (model, rung) reproduced the probe measurements *exactly* (identical X counts); only the B/S split moved by ≤2 cells, which is refusal-judge variance, not probe noise.

- **⚠️ A GREEN `--dry-run` IS NOT EVIDENCE THE RUN WILL START (found 2026-08-07 by job `8995805`, which died in 20 seconds after a queue wait).** `scripts/sae_pregate.py` called `guard_working_tree()` against a signature requiring `device`; all three H200 tasks failed instantly. Three things that looked like coverage were not: 982 tests were green but none invoke that script's `main()`; **`--dry-run` returns BEFORE the guard**, so it exercises argument parsing and a cost printout and no line of the real path — and the approval gate is built on `--dry-run`, so a preset can pass the gate on evidence touching none of the code; and `build_status.py` reported the instrument "built, tested and reachable", because reachability is an import-graph property that cannot see a wrong call. **Fix: `tests/test_entrypoint_call_sites.py`** statically binds every script's calls to watched library functions against their live signatures — cheap, no model, fails at `pytest` time instead of after a queue wait. **Grow `WATCHED` whenever a signature change breaks a caller**; that happened twice in one day (`strata` on `measure_deployment`, `device` on `guard_working_tree`). The same session's third instance of *a settled change that did not reach every caller*.

**Is the instrument build done? — do NOT sweep the tree by hand, run `uv run python scripts/build_status.py`** (keyless, no model, exit 1 while anything is outstanding). It reports the I0–I6 roster, the control battery and the untuned PLACEHOLDER knobs, DERIVING everything derivable — module existence, reachability from any entrypoint, placeholder markers — and declaring only the roster itself. Founded 2026-08-06 because that question was asked twice and both hand-sweeps were expensive, the second one wrong. `tests/test_completion.py` reconciles the declaration against the tree, so building an instrument and forgetting the manifest fails the build. **Scope is BUILD state, never claim state** — whether a number may appear in a paper is per-run and the contract already answers it (`reportable`/`withheld`); a finished instrument can still produce withheld readings all day. Detail: `instrument_build_plan.md` §5.1.

Run tests with `uv run pytest` (hermetic — no network, no keys); `uv run pytest -m slow` adds the real-weights smoke tests. Anything that calls a judge or downloads gated weights needs keys, so prefix it with the repo-root launcher: `./run python scripts/…`. Two scripts cost a run without launching it, both keyless and GPU-free: `--dry-run` on the pilot prints the work (capture passes, generations, judge calls), and `scripts/cost_model.py` turns that into the GPU-hours, dollars and wall-clock the approval gate requires — tokenising the real corpus under every rung rather than assuming an inflation factor. Assumptions live in `conf/cost.yaml`. **The first real run has now replaced them, and the pre-run estimate was badly optimistic: 0.7–2.4 GPU-hours per model was predicted; measured on H200 was ~4.5 h for Llama-3.1-8B (15 rungs, 4:30:43) and ~8+ h for Qwen2.5-7B (13 rungs in 8:00:26, killed at the wall) — 2–4× above the TOP of the predicted range, i.e. the error is in the throughput assumption, not the token census.** Per-rung: ~18 min Llama, ~32–37 min Qwen. Re-tune `conf/cost.yaml` from these before costing any later phase (TODO item 5); note also that one model exceeding the partition's 8 h wall means phase-0-scale work must be split across jobs by `--families`.

The pilot's corpus is four JSONL prompt sets copied from the guardrail sibling into the gitignored `data/`; a fresh clone re-copies them (`text_docs/project_structure.md` §5).

**THE COMPREHENSION BAND RAN 2026-08-05 (`band2-20260805`, jobs 8957794/8957795, both COMPLETED) — regime map of record: `text_docs/as5/band_20260805.md`.** 7 rungs x 200 prompts x 2 models, 57 min and 1 h 02 m, after a first attempt was killed at the 8 h wall by a BLAS thread-thrashing defect (fixed in `a0855fa`: probe fits are pinned single-threaded, which is 31x faster than the 8 threads an 8-CPU allocation takes by default; per-family flush+fsync now means a wall kill preserves completed rungs). What it establishes:

- **The gating question is answered more strongly than the pilot answered it.** On the four rungs where the instrument is sound — `zero_width`, `reverse_words`, `homoglyph`, `fullwidth` — **(B) is populated on both models** (Llama 7/1/1/11, Qwen 16/4/10/14) and (S) decode-and-refuse dominates everywhere. `fullwidth` yields the largest (B) cell yet measured and is the strongest phase-1 intervention substrate.
- **"Four EXACT comprehension-preserving rungs" is wrong for `tag_block`.** Measured ability: homoglyph 0.91/0.98, fullwidth 1.00/0.80, combining_marks 0.82/0.48, **tag_block 0.00/0.00**. `tag_block` is exactly invertible but unreadable — invertibility is a property of the encoding, not of the model's ability to read it, and the two came apart here for the first time. `reverse_characters` is likewise a genuine can't-decode rung (ability 0.00 on both; Llama restates "The quick brown fox jumps over the lazy dog").
- **⚠️ The open instrument defect is now the per-cell DEPLOYMENT READ, not licensing.** `harmless_rate` is exactly 0.50 on all 14 rungs because `probes.reading_percentile: 50.0` — the per-cell read has a **50% false-positive rate by construction**, and `assign_regime` consumes the boolean while never seeing the gap. Hard incoherence splits tenfold by probe strength (AUROC >= 0.80: mean 0.051, n=8; AUROC < 0.80: mean 0.540, n=6), and **every** hard incoherence in the run is `deployment_without_ability`. Permutation licensing does not prevent this: at n=200 an AUROC of 0.63 clears a shuffled-label null easily, so **significance is not sufficiency**. `meets_effect_size_bar` already exists and the regime assignment ignores it. AS-6 inherits this at its core.
- One case resists that explanation and may be real: **Qwen `fullwidth`, hard incoherence 0.20 on a probe with AUROC 1.00** — 20 cells with ability=False and a confidently-read content signal.

**INSTRUMENT-LAYER KNOWLEDGE HAS A SHARED CANONICAL HOME (founded 2026-08-05): `text_docs/shared/instrument_layer.md`.** Both papers CITE it; neither copies it. Anything that is a property of the MEASUREMENT rather than of a paper's object — probe licensing vs reading, the coherence rules, control calibration, capture positions, layer diagnostics, method imports — belongs there, and a finding written only into `text_docs/as5/` is a defect because AS-6 would re-derive it. This closes the namespace question `CLAUDE.md` left open at founding: the scheme is `text_docs/{as5,as6,shared}/`, matching the family convention. Why it is load-bearing for AS-6 specifically: **deployment is AS-6's central quantity** — AS-5 can lean on ability and behaviour if the deployment probe is weak, AS-6 cannot, because "the guard decoded it but did not flag it" is the entire paper.

**The band run was RE-READ against a control-calibrated floor the same day (`scripts/recalibrate_deployment.py`, CPU-only, no GPU, no spend; artifact `outputs/analysis/recalibration_20260805.json`).** Three results change what this repo believes:

- **The ability-0 rungs are a free negative control, and they set the noise floor.** Whatever the deployment probe reads on a rung the model cannot decode is by construction NOT decoded content, so its transfer AUROC there is the instrument's surface-feature floor: **0.656 (Llama), 0.671 (Qwen)**. The retired `auroc_threshold: 0.70` was approximately right *by luck* and now has a stated basis. Applying the floor collapses the hard incoherence that instrument-failed three rungs — below-floor rungs read `U` (unmeasured), not `X`. AS-6 must derive its OWN floor per guard, and should design can't-decode rungs into the guard-side ladder deliberately: they are the calibration, not waste.
- **⚠️ The earlier claim here that (D) has zero instances was an ARTEFACT of the median operating point.** At the strict read (D) is populated — Llama 9, Qwen 2 — while (B) shrinks (Llama 20→11, Qwen 45→40). So **(D) is real but rare (11 of 1,400 cells) and model-dependent; (C) is genuinely empty; (B) and (S) are robust.** The (B)/(D) split — Move B's stated contribution — currently rests on a threshold choice, so no (B)-vs-(D) number from this run may appear in a paper as a point estimate. Non-circular calibration criterion, and the one to use: hard incoherence falls MONOTONICALLY as the read tightens on both models (Llama 0.028→0.018, Qwen 0.136→0.088), so the coherence check endorses the strict read on its own consistency rather than on the counts it produces.
- **The sound rungs are THREE, not four — `reverse_words` is a control.** It permutes word order while leaving words intact, so the harmful tokens are lexically present and a content probe fires without any decoding; the layer index proves it, reading at L3–L5 rather than the L18–L22 `instruction_final` cluster that `zero_width`/`homoglyph`/`fullwidth` select on both models independently. **That layer split is itself a free validity check: late-mid + `instruction_final` = content recovered by computation; early + `last` = content present at the surface.** The general screen: a deployment probe measures decoding only when the encoding removes content from the lexical surface.

**THE BUILD SIDE HAS ITS OWN CANONICAL DOC (founded 2026-08-05): `text_docs/shared/instrument_build_plan.md`.** Sibling to `instrument_layer.md`: **that file is what the instrument layer has been FOUND to do; the build plan is what it WILL be.** Written after a comprehensive literature sweep on the owner's instruction ("write the docs of everything we want to build, and do another lit search — very comprehensive, do not rush to experiments"). It is canonical for the instrument roster (I0 capture spine · I1 logit lens · I2 trajectory · I3 entropy dynamics · I4 SAE · I5 reply-inversion causal test · I6 full causal toolkit), the seven binding design principles, the mandatory control battery, the tooling decision, the build sequence, and the annotated literature map. **Do not re-derive any of it, and do not copy it into a paper namespace.** Five things from that sweep that any session here must not re-learn:

- **The length confound is established literature, not just our observation.** arXiv 2605.00269 finds white-box OOD detection scores — naming CED, RAUQ and **WildGuard confidence**, i.e. one of AS-6's own guards — structurally confounded by sequence length at |r| ≥ 0.61 and collapsing to **chance (~0.50) under length-matched evaluation**. Our `measurements/length_null.py` and length-matched permutation strata are the same control, reached independently.
- **Trajectory analysis is promoted to a first-class instrument, and it costs no new capture.** The same paper reports **trajectory features retain genuine signal** (0.721 avg AUROC, 0.850 jailbreak) precisely on covert-intent inputs that share vocabulary with normal text — its "vocabulary-transparency spectrum" is our lexical-transparency finding with a published name, and our three sound rungs sit on that side of it. The curves are already persisted.
- **Our two capture positions have an established interpretation.** arXiv 2507.11878 (Northeastern + Stanford) studies exactly `t_inst`/`t_post-inst` = our `instruction_final`/`last`, and finds the first clusters by intrinsic **harmfulness**, the second by **behaviour**. It is also the **closest prior to AS-5's frame** — its jailbreaks all leave the text readable, so AS-5's delta is the *decode link* specifically and the harmfulness-vs-refusal split must be attributed to them; AS-6 must cite it too, since their Latent Guard already matches Llama Guard 3 8B.
- **A lens null is NOT proof of no decoding** — arXiv 2604.02608 shows function vectors that steer without being logit-lens readable. This is why the lens is built alongside I2–I4, not before them.
- **Llama Scope's SAEs are trained on Llama-3.1-8B-*Base*, our target is Instruct** — an Instruct-reconstruction pre-gate is mandatory before any SAE claim. Bonus for AS-6: Llama Guard 3 8B is itself a fine-tune of the same base, so one transfer validation may serve both papers.

**⚠️ THAT SWEEP WAS arXiv-ONLY, AND THE COVERAGE SWEEP THAT REPLACED IT IS THE RECORD: build plan §0 "Coverage". Read the correction below with it — the first reported version overstated the gap and named three papers as missed that were not.**

- **The real number: 473 peer-reviewed papers absent from the whole repo, 94 on-object.** (The first pass said 494/119 — it compared only against `instrument_build_plan.md` and `instrument_layer.md`, then reported the result as what the *project* had missed. This repo's literature knowledge also lives in `text_docs/as5/s1_idea_check.md`, `text_docs/as6/`, `text_docs/project_structure.md`, and module docstrings. 21 of the 494 were already cited somewhere.)
- **⚠️ Three headline "misses" were NOT missed, and any session repeating them is repeating a corrected error.** **2406.11717 Arditi** is cited in `probes/directions.py` line 1, `measurements/recognition.py` line 3, and four places in `as5/s1_idea_check.md` — including measurement #3's own table row and the phase-2 plan ("the Arditi trio: directional ablation for necessity, activation addition for sufficiency"). Its method was deliberately ported and attributed, not rediscovered. **2307.02483 Wei et al.** is cited by name in both `as6/` docs as the "mismatched generalization" source. **2406.04313 Circuit Breakers (Zou et al.)** is named in `project_structure.md` and `as5/s1_idea_check.md` as an objection to answer.
- **Seven genuine misses stand**, and they are the ones to act on: 2401.06102 Patchscopes (ICML, 233c), 2309.08600 SAEs (ICLR, 1432c), 2312.06681 CAA (ACL, 936c), 2309.16042 activation-patching best practices (ICLR, 294c), 2402.10260 StrongREJECT (NeurIPS, 356c), 2410.02707 (ICLR, 240c), 2301.04709 causal abstraction (JMLR, 183c).
- **The sequencing claim is narrowed accordingly.** What is true: the *instrument build plan* was written without seven canonical papers, and the arXiv-only sweep was a real defect (science ruled on it separately). What is NOT true, and was filed to science before being checked: that instruments were built "without knowing the source literature" — measurement #3's estimator was ported from Arditi on purpose at S1.
- **The lesson, which is the same one twice:** the first pass measured absence against a narrow index and reported it as absence in general. That is the failure the lit-search handbook already names — *a failed lookup is not evidence of absence* — committed one level up, against our own repo. **Before asserting the project does not know something, grep the whole repo, not the doc you happen to be editing.**
- The earlier "preprint-dominated subfield" premise is **refuted by measurement**: 561 of 712 resolved records (79%) sit at a named venue. That result is unaffected.

**THE THIRD CANONICAL SIBLING (founded 2026-08-06, settled same day): `text_docs/shared/pipeline_architecture.md` — how the code is ARRANGED.** `instrument_layer.md` = what the instruments were FOUND to do · `instrument_build_plan.md` = what WILL be built · this = how it is structured. Four decisions are settled there with their reasoning kept, and a session that reopens any of them is redoing work: **(1)** the **instrument contract is ADOPTED** — `measurements/contract.py` turns design principles P1–P7 into fields on a `Reading`, everything fails closed (an uncomputed control is never a cleared one, `licensed` is tri-state), `reportable` is the one place answering *may this number appear in a paper*, and `gate_per_prompt` is the granularity join from a condition-level verdict to the per-prompt axes both papers consume — the exact spot the deployment defect lived. **(2)** The proposed `measurements/licensing/combination` package split is **STRUCK, because its rationale was measured false**: those five modules already import no measurement sibling, so the decoupling existed and the split bought a directory name; the invariant is now `tests/test_package_structure.py`. **(3)** AS-6 uses the **same `Reading` type** — `assign_guard_cell` already took `decoded: bool | None` meaning "could not read this rung", i.e. `Reading.licensed` hand-rolled. **(4)** The run-record schema stays in `provenance.py`. ~~What remains is step 3 — extract the duplicated spine into `pipeline.py`~~ — **DONE 2026-08-06**; its selection rule is *the spine holds anything whose absence in ONE script would be a defect*, which is the length-null incident stated as a rule.

**A FIFTH decision was settled 2026-08-06 and it is the one a session building I4/I5/I6 needs: §3.5, where WRITE-side instruments plug in.** Every instrument up to I3 *reads* activations we captured, so it runs inside the family loop; the causal instruments *write* to the model, and that is a different shape — which is why `models/interventions.py` and `measurements/causal_license.py` sat built-and-unreachable for a day while I1–I3 were wired in an afternoon. **Causal licensing is a MODEL-level gate, not a rung-level instrument:** the direction is fit on the plain contrast sets, is identical for every rung, and gates which direction the downstream reads may use at all (TODO 28, from Arditi et al., NeurIPS 2024) — so it runs once between the plain captures and the family loop. Three rules came out of wiring it, each paid for by a defect: **a cost estimate must include the negative control** (the random-direction null's second sweep was briefly absent from `--dry-run` while the code already ran it); **a cost knob must be a CAP, not a stride** (a stride of 4 sweeps 7 layers of a 32-layer model and exactly ONE of a 3-layer model — layer 0, whose `resid_pre` is the raw embedding, so the whole sweep came back degenerate); and **a degenerate cell is a coverage number, not an exception** — `licensed=None` when nothing survives, because "no direction existed to intervene on" is not "harm is causally unmediated". The build plan's §6 step 8 is superseded for the licensing half only; I5's reply-inversion test and I6's patching toolkit keep their position.

**Next instrument move, method already identified — and its foundation CHANGED 2026-08-05:** replace the transferred content probe with a decode measurement built as a **Patchscopes instantiation** (arXiv 2401.06102, **ICML 2024, 233c** — the peer-reviewed framework for decoding hidden representations into natural language by patching them into a separate inference pass; the logit lens is an explicit special case), with **Fang & Marks (arXiv 2512.01222, tier (C), 4c** — logit lens recovers ROT-13 chain-of-thought from activations, peaking in intermediate-late layers) as the specific application rather than the foundation. This is what moves I1 off an unrefereed import; the §3.1 rewrite is filed, not yet made. It asks whether the plaintext tokens themselves become predictable from intermediate residual states, which takes the (B)/(D) split off the operating point entirely and carries a built-in negative control. It needs per-token lens readout, which the two-position capture spine cannot express — compute the lens inside the forward pass and store only the expected-token probability (~640K floats) rather than caching per-token residuals (~5 GB/condition). Full detail and AS-6 inheritance: `text_docs/shared/instrument_layer.md` §4.

## Conventions

- **Package manager is `uv`** (global law); `uv.lock` is committed. No application frameworks, CLI structure, or trackers assumed before discussion — whether this project ever needs one is deferred until the phase-0 pilot shows the real run shapes (`text_docs/project_structure.md` §7.3).
- **Artifact layout:** `outputs/activations/` is a capture cache reusable across analyses; `outputs/runs/<phase>/<model>/<run_name>/` holds one dir per run with its `results.json` provenance record. Both gitignored; both defined in `src/internals_safety/paths.py`.
- **LLM provider layer:** the `llm_utils` base package as a pinned git dep by tag (family convention; never vendor a copy) — pinned at `v5.0.0`, the same tag as the guardrail sibling, so judge behaviour matches across the family.
- **API keys** reach the process from the secret manager at launch, via the gitignored repo-root `run` wrapper and its pointer map. Nothing vault-shaped is ever committed; no plaintext key touches disk. The same wrapper is the cluster path.
- **Experiment-run approval gate (family rule, owner 2026-07-22):** training and intervention runs are heavy — BEFORE launching ANY run, report an explicit estimate of (1) GPU count + type, (2) money ($), and (3) wall-clock time, and get the owner's explicit go.
- **Cluster runs go through a committed PRESET, never a hand-typed flag string (settled 2026-08-07).** `conf/experiment/<name>.yaml` declares the run; `scripts/submit.py <preset>` prints it and submits NOTHING without `--submit`; `ops/run.sbatch` is the one generic launcher. The three-command loop is `submit.py <preset>` → `cost_model.py --preset <preset>` → the owner's go → `--submit`. **Do not write a new `*.sbatch` per experiment** — that is what this replaced, and two of the three it replaced existed only on the cluster, never in git. Two schema properties are load-bearing and must not be relaxed: the schema is CLOSED (no preset may carry a `measurements.yaml` knob — enforced by `tests/test_presets.py`), and `gates:` is REQUIRED, so a job cannot be constructed until *what would I build differently depending on the result?* is answered in a committed file. Local work keeps argparse.
- **⚠️ A run must be a GATE, not a measurement, until the instrument roster is complete (owner defect report 2026-08-06; problem report filed as science TODO item 18, science rules on the general law).** The test, applied BEFORE launch: *what would I build differently depending on the result?* No answer means the run gates nothing, and a run that gates nothing waits until the instruments are done. This is **not** the S6b grounding rule and S6b does not cover it — every instrument can be grounded and a session can still choose a non-blocking run over continuing the build, which is what happened on 2026-08-05: the AS-6 phase-1 sweep and the comprehension band both ran after the gating question was already answered (`4453bed`), and the instrument build plan naming what to build (`00da340`) arrived after both. Cost: thirteen instrument-repair commits in one day all downstream of runs, every quantitative map from both runs revised at least once, and a roster that **as of 2026-08-06** still had I4/I5 unbuilt, I6 partial, six modules wired to zero scripts, and 4 of 9 mandatory controls missing. *(State as of 2026-08-07, from `build_status.py` — do not re-sweep by hand: I0–I3, I5, I6 all complete, control battery 7/7, and **I4 is the single remaining roster item**. Its unbuilt half is deliberately gated on the SAE pre-gate, which needs the 8B Base arm — cluster work. The rule stands; what has changed is that finishing the roster now REQUIRES a run rather than being blocked by one.)* **The empirical basis for the rule, from this repo's own data:** the pilot's GATE answer ((B) is populated) survived every subsequent instrument fix unchanged, while every quantitative MAP from the same runs was revised — a go/no-go is robust to instrument quality in a way a measurement is not.
- **Compute options — this repo has been designed around ONE of three, and that is now a live question (noted 2026-08-05).** Every design decision here assumes **NURC/Explorer**: free, but `normal` QOS = **1 GPU per job, 8 h wall, 4 concurrent, multi-GPU never schedules**. That constraint is why phase-0 must be split across jobs by `--families`, and the pilot still **hit the wall** (Qwen killed at 8:00:26). The family also has the **xc cluster** — a shared single-node SLURM box, partition `main`, **8×A100-80GB with NO time limit** — and **AICR**. xc is not currently referenced anywhere in this repo's design, cost model or `ops/`. Canonical facts live in the devices repo `knowledge/clusters/` (`xc_cluster_properties.md`, `nurc_cluster_properties.md`, `aicr_cluster_properties.md`; moved from science 2026-08-05); the job-portability standard is `knowledge/cluster_job_standard.md` there. **Never write any cluster's host, IP, key, or a box owner's name into this repo — it is public; the sanctioned token is "xc cluster" plus a pointer to the devices repo** (owner rule 2026-07-18). Whether this project's later phases move to xc is an open owner decision, filed in TODO.
- **Cluster sync (family standard, settled 2026-08-02):** git clone/pull for committed code + rsync/scp for the gitignored ops layer (`*.sbatch`, cluster configs) + rsync-down for results — canonical: devices repo `knowledge/cluster_sync_convention.md` (moved from the science organ 2026-08-05). This repo is public, so the code path is plain `git clone` (no auth); wire the down-sync sites + ops-file rsync at S2 when code lands. Never fork a per-repo cluster-sync scheme.
- **Conference deadlines:** the canonical timeline lives in `llm_guardrail_security/text_docs/shared/conference_timeline.md` — consult and update THERE, never fork a per-repo deadline list.
- **Public-grade discipline (mandatory):** no secrets / PII / secret-manager or vault references in any committed file; `TODO.md`, `NOW.md`, `knowledge/`, `outputs/`, `data/`, `text_docs/reviews/` are gitignored.
- **English only** in task files (human names may stay as-is).

## Skills installed

- `reproducible-run-logging` — the write side of reproducibility: the canonical `results.json` run-record schema and the dirty-tree guard. Installed 2026-08-02, deliberately *before* the first entrypoint exists, so `scripts/phase0_regime_map.py` is born emitting it.

Not installed yet, with its trigger named: `check-experiment-results` (the read/triage side) — it needs a job runner, per-run output dirs, and a results doc to triage, none of which exist before the first cluster runs. Install it in the same sitting as the first phase-0 job.

## Task system

Root `TODO.md` (gitignored — task text is personal), psyche task standard (position = priority; finished items move to the central psyche archive). Registered in the psyche oikos map as a research bet. First-paper ideation is tracked in the science repo's TODO (ideation runs from science sessions).
